import React from 'react';
import {View, Text, TouchableOpacity} from 'react-native';

const Posts = ({}) => {
    return (
        <View>
            <Text>Posts</Text>
        </View>
    );
};

export default Posts;
