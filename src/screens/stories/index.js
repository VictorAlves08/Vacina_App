import React from 'react';
import {View, Text, TouchableOpacity} from 'react-native';

const Stories = ({}) => {
    return (
        <View>
            <Text>Stories</Text>
        </View>
    );
};

export default Stories;
